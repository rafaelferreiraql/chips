// Made just in case the first route (ES6 classes) doesn't work out

let player = {
    test: function() {console.log(Graphics);}
}

let player1 = Object.create(player);
let player2 = Object.create(player);
